apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: local-mysql-dados
spec:
  storageClassName: manual
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
spec:
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - image: elvismc/projeto-database:1.0
        args:
        - "--ignore-db-dir=lost+found"
        imagePullPolicy: Always
        name: mysql
        ports:
        - containerPort: 3306
          name: mysql

        volumeMounts:
        - name: local-mysql-dados
          mountPath: /var/lib/mysql/
      volumes:
      - name: local-mysql-dados
        persistentVolumeClaim:
          claimName: local-mysql-dados

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: php
  labels:
    app: php
spec:
  replicas: 6
  selector:
    matchLabels:
      app: php
  template:
    metadata:
      labels:
        app: php
    spec:
      containers:
      - name: php
        image: elvismc/projeto-backend:1.0
        imagePullPolicy: Always
        ports:
        - containerPort: 80

